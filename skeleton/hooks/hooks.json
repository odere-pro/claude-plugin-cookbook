{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}\"/hooks/example-hook.sh",
            "description": "Example PostToolUse hook — logs each Write/Edit. Replace or remove."
          }
        ]
      }
    ]
  }
}
